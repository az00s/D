@model D.Models.IndexViewModel
@{
    ViewBag.Title = "Кабинет пользователя";
}


<p class="text-success">@ViewBag.StatusMessage</p>
<div class="row">
    <div class="col-lg-2 col-xs-0"></div>
    <div class="col-lg-9 col-lg-offset-0"><h4>Вы можете изменить ваш пароль:</h4><hr /></div>
    <div class="col-lg-1 col-xs-0"></div>
    </div>
        <div class="row">
            <div class="col-lg-2 col-xs-0"></div>
            <div class="col-lg-9 col-lg-offset-0">
   Пароль:
    
        [
        @if (Model.HasPassword)
            {
            @Html.ActionLink("Изменить пароль", "ChangePassword")
        }
        else
        {
            @Html.ActionLink("Новый пароль", "SetPassword")
        }
        ]
    
</div>
            <div class="col-lg-1 col-xs-0"></div>
            
            
        </div>
